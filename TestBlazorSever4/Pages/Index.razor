@page "/"
@using System.ComponentModel;
@using System.Runtime;
@implements IDisposable

<PageTitle>Index</PageTitle>

<h3>Grid</h3>

<label>
    <input type="checkbox" @bind="GC_Collect" />
    GC Collect Manually
</label>

<TelerikGrid @ref="@GridRef"
             Data="@GridData"
             Pageable="true"
             PageSize="10"
             Sortable="true"
             ConfirmDelete="true"
             SortMode="@SortMode.Multiple"
             FilterMode="@GridFilterMode.FilterRow"
             Width="100%"
             Class="userprofilelisttable table-wrapper data-grid-table-wrapper">
    <GridColumns>
        <GridCommandColumn Title="" Width="5%">
            <GridCommandButton Command="Edit" Title="Edit"
                               ThemeColor=""
                               class="table-btn-icons"
                               OnClick="@((args)=>OnPLSEdit(args.Item as PLSRequestModel))">
                <div class="edit-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
        <GridColumn Field="AssociatedDTROrIRNumber"
                    Title="AssociatedDTROrIRNumberLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel dtrnumber = (PLSRequestModel)context;
                    <div id="target" title="@dtrnumber.AssociatedDTROrIRNumber">
                        @dtrnumber.AssociatedDTROrIRNumber
                    </div>
                }
            </Template>

            <HeaderTemplate>
                <div title=AssociatedDTROrIRNumberLabel>
                    AssociatedDTROrIRNumberLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="SRNumber"
                    Title="SRNumber"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel srnumber = (PLSRequestModel)context;
                    <div id="target" title="@srnumber.SRNumber">
                        @srnumber.SRNumber
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=SRNumber>
                    SRNumber
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="DateSubmitted"
                    Title="DateSubmitted"
                    ShowFilterCellButtons="false"
                    Filterable="true" DisplayFormat="{0:dd/MMM/yyyy}" Width="17%">
            <Template>
                @{
                    PLSRequestModel datesubmitted = (PLSRequestModel)context;
                    <div id="target" title="@datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")">
                        @datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=DateSubmitted>
                    Model.DateSubmitted
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="ProjectTitle"
                    Title="ProjectTitle"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel projecttitle = (PLSRequestModel)context;
                    <div id="target" title="@projecttitle.ProjectTitle">
                        @projecttitle.ProjectTitle
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=ProjectTitle>
                    ProjectTitle
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTFundingProgramText"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="16%"
                    Title="CTFundingProgramLabel">
            <Template>
                @{
                    PLSRequestModel ctfundingprgm = (PLSRequestModel)context;
                    <div id="target" title="@ctfundingprgm.CTFundingProgramText">
                        @ctfundingprgm.CTFundingProgramText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTFundingProgramLabel>
                    CTFundingProgramLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="FundingCountryOrProject"
                    Title="FundingCountryOrProjectLabel"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel fundingcp = (PLSRequestModel)context;
                    <div id="target" title="@fundingcp.FundingCountryOrProject">
                        @fundingcp.FundingCountryOrProject
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=FundingCountryOrProjectLabel>
                    FundingCountryOrProjectLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTRequestorName"
                    Title="CTRequestorNameLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="15%">
            <Template>
                @{
                    PLSRequestModel ctrequestorname = (PLSRequestModel)context;
                    <div id="target" title=" @ctrequestorname.CTRequestorName">
                        @ctrequestorname.CTRequestorName
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTRequestorNameLabel>
                    CTRequestorNameLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="StartDate"
                    Title="StartDate"
                    ShowFilterCellButtons="false" DisplayFormat="{0:dd/MMM/yyyy}"
                    Filterable="true" Width="16%">
            <Template>
                @{
                    PLSRequestModel startdate = (PLSRequestModel)context;
                    <div id="target" title="@startdate.StartDate?.ToString("dd-MMM-yyyy")">
                        @startdate.StartDate?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=StartDate>
                    StartDate
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CurrentStatusText"
                    Title="CurrentStatusLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="8%">
            <Template>
                @{
                    PLSRequestModel currentstatus = (PLSRequestModel)context;
                    <div id="target" title=" @currentstatus.CurrentStatusText">
                        @currentstatus.CurrentStatusText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CurrentStatusLabel>
                    CurrentStatusLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="RedFlagText"
                    Title="RegFlagLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="9%">
            <Template>
                @{
                    PLSRequestModel redflag = (PLSRequestModel)context;
                    <div id="target" title="@redflag.RedFlagText">
                        @redflag.RedFlagText
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=RegFlagLabel>
                    RegFlagLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridCommandColumn Title="" Width="12%">
            <GridCommandButton Command="Copy" Title="Copy"
                               ThemeColor=""
                               OnClick="@((args)=> OnPLSCopy(args.Item as PLSRequestModel))"
                               class="table-btn-icons-custom">
                <div class="copy-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
            <GridCommandButton Command="Delete" Title="Delete"
                               ThemeColor=""
                               class="table-btn-icons table-trash-icon">
                <div class="inactive-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-delete">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
    </GridColumns>
</TelerikGrid>

<TelerikGrid @ref="@GridRef_1"
             Data="@GridData"
             Pageable="true"
             PageSize="10"
             Sortable="true"
             ConfirmDelete="true"
             SortMode="@SortMode.Multiple"
             FilterMode="@GridFilterMode.FilterRow"
             Width="100%"
             Class="userprofilelisttable table-wrapper data-grid-table-wrapper">
    <GridColumns>
        <GridCommandColumn Title="" Width="5%">
            <GridCommandButton Command="Edit" Title="Edit"
                               ThemeColor=""
                               class="table-btn-icons"
                               OnClick="@((args)=>OnPLSEdit(args.Item as PLSRequestModel))">
                <div class="edit-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
        <GridColumn Field="AssociatedDTROrIRNumber"
                    Title="AssociatedDTROrIRNumberLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel dtrnumber = (PLSRequestModel)context;
                    <div id="target" title="@dtrnumber.AssociatedDTROrIRNumber">
                        @dtrnumber.AssociatedDTROrIRNumber
                    </div>
                }
            </Template>

            <HeaderTemplate>
                <div title=AssociatedDTROrIRNumberLabel>
                    AssociatedDTROrIRNumberLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="SRNumber"
                    Title="SRNumber"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel srnumber = (PLSRequestModel)context;
                    <div id="target" title="@srnumber.SRNumber">
                        @srnumber.SRNumber
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=SRNumber>
                    SRNumber
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="DateSubmitted"
                    Title="DateSubmitted"
                    ShowFilterCellButtons="false"
                    Filterable="true" DisplayFormat="{0:dd/MMM/yyyy}" Width="17%">
            <Template>
                @{
                    PLSRequestModel datesubmitted = (PLSRequestModel)context;
                    <div id="target" title="@datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")">
                        @datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=DateSubmitted>
                    Model.DateSubmitted
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="ProjectTitle"
                    Title="ProjectTitle"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel projecttitle = (PLSRequestModel)context;
                    <div id="target" title="@projecttitle.ProjectTitle">
                        @projecttitle.ProjectTitle
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=ProjectTitle>
                    ProjectTitle
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTFundingProgramText"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="16%"
                    Title="CTFundingProgramLabel">
            <Template>
                @{
                    PLSRequestModel ctfundingprgm = (PLSRequestModel)context;
                    <div id="target" title="@ctfundingprgm.CTFundingProgramText">
                        @ctfundingprgm.CTFundingProgramText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTFundingProgramLabel>
                    CTFundingProgramLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="FundingCountryOrProject"
                    Title="FundingCountryOrProjectLabel"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel fundingcp = (PLSRequestModel)context;
                    <div id="target" title="@fundingcp.FundingCountryOrProject">
                        @fundingcp.FundingCountryOrProject
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=FundingCountryOrProjectLabel>
                    FundingCountryOrProjectLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTRequestorName"
                    Title="CTRequestorNameLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="15%">
            <Template>
                @{
                    PLSRequestModel ctrequestorname = (PLSRequestModel)context;
                    <div id="target" title=" @ctrequestorname.CTRequestorName">
                        @ctrequestorname.CTRequestorName
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTRequestorNameLabel>
                    CTRequestorNameLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="StartDate"
                    Title="StartDate"
                    ShowFilterCellButtons="false" DisplayFormat="{0:dd/MMM/yyyy}"
                    Filterable="true" Width="16%">
            <Template>
                @{
                    PLSRequestModel startdate = (PLSRequestModel)context;
                    <div id="target" title="@startdate.StartDate?.ToString("dd-MMM-yyyy")">
                        @startdate.StartDate?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=StartDate>
                    StartDate
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CurrentStatusText"
                    Title="CurrentStatusLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="8%">
            <Template>
                @{
                    PLSRequestModel currentstatus = (PLSRequestModel)context;
                    <div id="target" title=" @currentstatus.CurrentStatusText">
                        @currentstatus.CurrentStatusText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CurrentStatusLabel>
                    CurrentStatusLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="RedFlagText"
                    Title="RegFlagLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="9%">
            <Template>
                @{
                    PLSRequestModel redflag = (PLSRequestModel)context;
                    <div id="target" title="@redflag.RedFlagText">
                        @redflag.RedFlagText
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=RegFlagLabel>
                    RegFlagLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridCommandColumn Title="" Width="12%">
            <GridCommandButton Command="Copy" Title="Copy"
                               ThemeColor=""
                               OnClick="@((args)=> OnPLSCopy(args.Item as PLSRequestModel))"
                               class="table-btn-icons-custom">
                <div class="copy-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
            <GridCommandButton Command="Delete" Title="Delete"
                               ThemeColor=""
                               class="table-btn-icons table-trash-icon">
                <div class="inactive-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-delete">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
    </GridColumns>
</TelerikGrid>
<TelerikGrid @ref="@GridRef_2"
             Data="@GridData"
             Pageable="true"
             PageSize="10"
             Sortable="true"
             ConfirmDelete="true"
             SortMode="@SortMode.Multiple"
             FilterMode="@GridFilterMode.FilterRow"
             Width="100%"
             Class="userprofilelisttable table-wrapper data-grid-table-wrapper">
    <GridColumns>
        <GridCommandColumn Title="" Width="5%">
            <GridCommandButton Command="Edit" Title="Edit"
                               ThemeColor=""
                               class="table-btn-icons"
                               OnClick="@((args)=>OnPLSEdit(args.Item as PLSRequestModel))">
                <div class="edit-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
        <GridColumn Field="AssociatedDTROrIRNumber"
                    Title="AssociatedDTROrIRNumberLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel dtrnumber = (PLSRequestModel)context;
                    <div id="target" title="@dtrnumber.AssociatedDTROrIRNumber">
                        @dtrnumber.AssociatedDTROrIRNumber
                    </div>
                }
            </Template>

            <HeaderTemplate>
                <div title=AssociatedDTROrIRNumberLabel>
                    AssociatedDTROrIRNumberLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="SRNumber"
                    Title="SRNumber"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel srnumber = (PLSRequestModel)context;
                    <div id="target" title="@srnumber.SRNumber">
                        @srnumber.SRNumber
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=SRNumber>
                    SRNumber
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="DateSubmitted"
                    Title="DateSubmitted"
                    ShowFilterCellButtons="false"
                    Filterable="true" DisplayFormat="{0:dd/MMM/yyyy}" Width="17%">
            <Template>
                @{
                    PLSRequestModel datesubmitted = (PLSRequestModel)context;
                    <div id="target" title="@datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")">
                        @datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=DateSubmitted>
                    Model.DateSubmitted
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="ProjectTitle"
                    Title="ProjectTitle"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel projecttitle = (PLSRequestModel)context;
                    <div id="target" title="@projecttitle.ProjectTitle">
                        @projecttitle.ProjectTitle
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=ProjectTitle>
                    ProjectTitle
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTFundingProgramText"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="16%"
                    Title="CTFundingProgramLabel">
            <Template>
                @{
                    PLSRequestModel ctfundingprgm = (PLSRequestModel)context;
                    <div id="target" title="@ctfundingprgm.CTFundingProgramText">
                        @ctfundingprgm.CTFundingProgramText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTFundingProgramLabel>
                    CTFundingProgramLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="FundingCountryOrProject"
                    Title="FundingCountryOrProjectLabel"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel fundingcp = (PLSRequestModel)context;
                    <div id="target" title="@fundingcp.FundingCountryOrProject">
                        @fundingcp.FundingCountryOrProject
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=FundingCountryOrProjectLabel>
                    FundingCountryOrProjectLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTRequestorName"
                    Title="CTRequestorNameLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="15%">
            <Template>
                @{
                    PLSRequestModel ctrequestorname = (PLSRequestModel)context;
                    <div id="target" title=" @ctrequestorname.CTRequestorName">
                        @ctrequestorname.CTRequestorName
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTRequestorNameLabel>
                    CTRequestorNameLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="StartDate"
                    Title="StartDate"
                    ShowFilterCellButtons="false" DisplayFormat="{0:dd/MMM/yyyy}"
                    Filterable="true" Width="16%">
            <Template>
                @{
                    PLSRequestModel startdate = (PLSRequestModel)context;
                    <div id="target" title="@startdate.StartDate?.ToString("dd-MMM-yyyy")">
                        @startdate.StartDate?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=StartDate>
                    StartDate
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CurrentStatusText"
                    Title="CurrentStatusLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="8%">
            <Template>
                @{
                    PLSRequestModel currentstatus = (PLSRequestModel)context;
                    <div id="target" title=" @currentstatus.CurrentStatusText">
                        @currentstatus.CurrentStatusText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CurrentStatusLabel>
                    CurrentStatusLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="RedFlagText"
                    Title="RegFlagLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="9%">
            <Template>
                @{
                    PLSRequestModel redflag = (PLSRequestModel)context;
                    <div id="target" title="@redflag.RedFlagText">
                        @redflag.RedFlagText
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=RegFlagLabel>
                    RegFlagLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridCommandColumn Title="" Width="12%">
            <GridCommandButton Command="Copy" Title="Copy"
                               ThemeColor=""
                               OnClick="@((args)=> OnPLSCopy(args.Item as PLSRequestModel))"
                               class="table-btn-icons-custom">
                <div class="copy-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
            <GridCommandButton Command="Delete" Title="Delete"
                               ThemeColor=""
                               class="table-btn-icons table-trash-icon">
                <div class="inactive-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-delete">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
    </GridColumns>
</TelerikGrid>
<TelerikGrid @ref="@GridRef_3"
             Data="@GridData"
             Pageable="true"
             PageSize="10"
             Sortable="true"
             ConfirmDelete="true"
             SortMode="@SortMode.Multiple"
             FilterMode="@GridFilterMode.FilterRow"
             Width="100%"
             Class="userprofilelisttable table-wrapper data-grid-table-wrapper">
    <GridColumns>
        <GridCommandColumn Title="" Width="5%">
            <GridCommandButton Command="Edit" Title="Edit"
                               ThemeColor=""
                               class="table-btn-icons"
                               OnClick="@((args)=>OnPLSEdit(args.Item as PLSRequestModel))">
                <div class="edit-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
        <GridColumn Field="AssociatedDTROrIRNumber"
                    Title="AssociatedDTROrIRNumberLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel dtrnumber = (PLSRequestModel)context;
                    <div id="target" title="@dtrnumber.AssociatedDTROrIRNumber">
                        @dtrnumber.AssociatedDTROrIRNumber
                    </div>
                }
            </Template>

            <HeaderTemplate>
                <div title=AssociatedDTROrIRNumberLabel>
                    AssociatedDTROrIRNumberLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="SRNumber"
                    Title="SRNumber"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel srnumber = (PLSRequestModel)context;
                    <div id="target" title="@srnumber.SRNumber">
                        @srnumber.SRNumber
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=SRNumber>
                    SRNumber
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="DateSubmitted"
                    Title="DateSubmitted"
                    ShowFilterCellButtons="false"
                    Filterable="true" DisplayFormat="{0:dd/MMM/yyyy}" Width="17%">
            <Template>
                @{
                    PLSRequestModel datesubmitted = (PLSRequestModel)context;
                    <div id="target" title="@datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")">
                        @datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=DateSubmitted>
                    Model.DateSubmitted
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="ProjectTitle"
                    Title="ProjectTitle"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel projecttitle = (PLSRequestModel)context;
                    <div id="target" title="@projecttitle.ProjectTitle">
                        @projecttitle.ProjectTitle
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=ProjectTitle>
                    ProjectTitle
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTFundingProgramText"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="16%"
                    Title="CTFundingProgramLabel">
            <Template>
                @{
                    PLSRequestModel ctfundingprgm = (PLSRequestModel)context;
                    <div id="target" title="@ctfundingprgm.CTFundingProgramText">
                        @ctfundingprgm.CTFundingProgramText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTFundingProgramLabel>
                    CTFundingProgramLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="FundingCountryOrProject"
                    Title="FundingCountryOrProjectLabel"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel fundingcp = (PLSRequestModel)context;
                    <div id="target" title="@fundingcp.FundingCountryOrProject">
                        @fundingcp.FundingCountryOrProject
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=FundingCountryOrProjectLabel>
                    FundingCountryOrProjectLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTRequestorName"
                    Title="CTRequestorNameLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="15%">
            <Template>
                @{
                    PLSRequestModel ctrequestorname = (PLSRequestModel)context;
                    <div id="target" title=" @ctrequestorname.CTRequestorName">
                        @ctrequestorname.CTRequestorName
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTRequestorNameLabel>
                    CTRequestorNameLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="StartDate"
                    Title="StartDate"
                    ShowFilterCellButtons="false" DisplayFormat="{0:dd/MMM/yyyy}"
                    Filterable="true" Width="16%">
            <Template>
                @{
                    PLSRequestModel startdate = (PLSRequestModel)context;
                    <div id="target" title="@startdate.StartDate?.ToString("dd-MMM-yyyy")">
                        @startdate.StartDate?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=StartDate>
                    StartDate
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CurrentStatusText"
                    Title="CurrentStatusLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="8%">
            <Template>
                @{
                    PLSRequestModel currentstatus = (PLSRequestModel)context;
                    <div id="target" title=" @currentstatus.CurrentStatusText">
                        @currentstatus.CurrentStatusText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CurrentStatusLabel>
                    CurrentStatusLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="RedFlagText"
                    Title="RegFlagLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="9%">
            <Template>
                @{
                    PLSRequestModel redflag = (PLSRequestModel)context;
                    <div id="target" title="@redflag.RedFlagText">
                        @redflag.RedFlagText
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=RegFlagLabel>
                    RegFlagLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridCommandColumn Title="" Width="12%">
            <GridCommandButton Command="Copy" Title="Copy"
                               ThemeColor=""
                               OnClick="@((args)=> OnPLSCopy(args.Item as PLSRequestModel))"
                               class="table-btn-icons-custom">
                <div class="copy-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
            <GridCommandButton Command="Delete" Title="Delete"
                               ThemeColor=""
                               class="table-btn-icons table-trash-icon">
                <div class="inactive-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-delete">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
    </GridColumns>
</TelerikGrid>
<TelerikGrid @ref="@GridRef_4"
             Data="@GridData"
             Pageable="true"
             PageSize="10"
             Sortable="true"
             ConfirmDelete="true"
             SortMode="@SortMode.Multiple"
             FilterMode="@GridFilterMode.FilterRow"
             Width="100%"
             Class="userprofilelisttable table-wrapper data-grid-table-wrapper">
    <GridColumns>
        <GridCommandColumn Title="" Width="5%">
            <GridCommandButton Command="Edit" Title="Edit"
                               ThemeColor=""
                               class="table-btn-icons"
                               OnClick="@((args)=>OnPLSEdit(args.Item as PLSRequestModel))">
                <div class="edit-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
        <GridColumn Field="AssociatedDTROrIRNumber"
                    Title="AssociatedDTROrIRNumberLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel dtrnumber = (PLSRequestModel)context;
                    <div id="target" title="@dtrnumber.AssociatedDTROrIRNumber">
                        @dtrnumber.AssociatedDTROrIRNumber
                    </div>
                }
            </Template>

            <HeaderTemplate>
                <div title=AssociatedDTROrIRNumberLabel>
                    AssociatedDTROrIRNumberLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="SRNumber"
                    Title="SRNumber"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel srnumber = (PLSRequestModel)context;
                    <div id="target" title="@srnumber.SRNumber">
                        @srnumber.SRNumber
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=SRNumber>
                    SRNumber
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="DateSubmitted"
                    Title="DateSubmitted"
                    ShowFilterCellButtons="false"
                    Filterable="true" DisplayFormat="{0:dd/MMM/yyyy}" Width="17%">
            <Template>
                @{
                    PLSRequestModel datesubmitted = (PLSRequestModel)context;
                    <div id="target" title="@datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")">
                        @datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=DateSubmitted>
                    Model.DateSubmitted
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="ProjectTitle"
                    Title="ProjectTitle"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel projecttitle = (PLSRequestModel)context;
                    <div id="target" title="@projecttitle.ProjectTitle">
                        @projecttitle.ProjectTitle
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=ProjectTitle>
                    ProjectTitle
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTFundingProgramText"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="16%"
                    Title="CTFundingProgramLabel">
            <Template>
                @{
                    PLSRequestModel ctfundingprgm = (PLSRequestModel)context;
                    <div id="target" title="@ctfundingprgm.CTFundingProgramText">
                        @ctfundingprgm.CTFundingProgramText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTFundingProgramLabel>
                    CTFundingProgramLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="FundingCountryOrProject"
                    Title="FundingCountryOrProjectLabel"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel fundingcp = (PLSRequestModel)context;
                    <div id="target" title="@fundingcp.FundingCountryOrProject">
                        @fundingcp.FundingCountryOrProject
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=FundingCountryOrProjectLabel>
                    FundingCountryOrProjectLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTRequestorName"
                    Title="CTRequestorNameLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="15%">
            <Template>
                @{
                    PLSRequestModel ctrequestorname = (PLSRequestModel)context;
                    <div id="target" title=" @ctrequestorname.CTRequestorName">
                        @ctrequestorname.CTRequestorName
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTRequestorNameLabel>
                    CTRequestorNameLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="StartDate"
                    Title="StartDate"
                    ShowFilterCellButtons="false" DisplayFormat="{0:dd/MMM/yyyy}"
                    Filterable="true" Width="16%">
            <Template>
                @{
                    PLSRequestModel startdate = (PLSRequestModel)context;
                    <div id="target" title="@startdate.StartDate?.ToString("dd-MMM-yyyy")">
                        @startdate.StartDate?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=StartDate>
                    StartDate
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CurrentStatusText"
                    Title="CurrentStatusLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="8%">
            <Template>
                @{
                    PLSRequestModel currentstatus = (PLSRequestModel)context;
                    <div id="target" title=" @currentstatus.CurrentStatusText">
                        @currentstatus.CurrentStatusText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CurrentStatusLabel>
                    CurrentStatusLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="RedFlagText"
                    Title="RegFlagLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="9%">
            <Template>
                @{
                    PLSRequestModel redflag = (PLSRequestModel)context;
                    <div id="target" title="@redflag.RedFlagText">
                        @redflag.RedFlagText
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=RegFlagLabel>
                    RegFlagLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridCommandColumn Title="" Width="12%">
            <GridCommandButton Command="Copy" Title="Copy"
                               ThemeColor=""
                               OnClick="@((args)=> OnPLSCopy(args.Item as PLSRequestModel))"
                               class="table-btn-icons-custom">
                <div class="copy-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
            <GridCommandButton Command="Delete" Title="Delete"
                               ThemeColor=""
                               class="table-btn-icons table-trash-icon">
                <div class="inactive-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-delete">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
    </GridColumns>
</TelerikGrid>
<TelerikGrid @ref="@GridRef_5"
             Data="@GridData"
             Pageable="true"
             PageSize="10"
             Sortable="true"
             ConfirmDelete="true"
             SortMode="@SortMode.Multiple"
             FilterMode="@GridFilterMode.FilterRow"
             Width="100%"
             Class="userprofilelisttable table-wrapper data-grid-table-wrapper">
    <GridColumns>
        <GridCommandColumn Title="" Width="5%">
            <GridCommandButton Command="Edit" Title="Edit"
                               ThemeColor=""
                               class="table-btn-icons"
                               OnClick="@((args)=>OnPLSEdit(args.Item as PLSRequestModel))">
                <div class="edit-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
        <GridColumn Field="AssociatedDTROrIRNumber"
                    Title="AssociatedDTROrIRNumberLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel dtrnumber = (PLSRequestModel)context;
                    <div id="target" title="@dtrnumber.AssociatedDTROrIRNumber">
                        @dtrnumber.AssociatedDTROrIRNumber
                    </div>
                }
            </Template>

            <HeaderTemplate>
                <div title=AssociatedDTROrIRNumberLabel>
                    AssociatedDTROrIRNumberLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="SRNumber"
                    Title="SRNumber"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel srnumber = (PLSRequestModel)context;
                    <div id="target" title="@srnumber.SRNumber">
                        @srnumber.SRNumber
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=SRNumber>
                    SRNumber
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="DateSubmitted"
                    Title="DateSubmitted"
                    ShowFilterCellButtons="false"
                    Filterable="true" DisplayFormat="{0:dd/MMM/yyyy}" Width="17%">
            <Template>
                @{
                    PLSRequestModel datesubmitted = (PLSRequestModel)context;
                    <div id="target" title="@datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")">
                        @datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=DateSubmitted>
                    Model.DateSubmitted
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="ProjectTitle"
                    Title="ProjectTitle"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel projecttitle = (PLSRequestModel)context;
                    <div id="target" title="@projecttitle.ProjectTitle">
                        @projecttitle.ProjectTitle
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=ProjectTitle>
                    ProjectTitle
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTFundingProgramText"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="16%"
                    Title="CTFundingProgramLabel">
            <Template>
                @{
                    PLSRequestModel ctfundingprgm = (PLSRequestModel)context;
                    <div id="target" title="@ctfundingprgm.CTFundingProgramText">
                        @ctfundingprgm.CTFundingProgramText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTFundingProgramLabel>
                    CTFundingProgramLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="FundingCountryOrProject"
                    Title="FundingCountryOrProjectLabel"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel fundingcp = (PLSRequestModel)context;
                    <div id="target" title="@fundingcp.FundingCountryOrProject">
                        @fundingcp.FundingCountryOrProject
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=FundingCountryOrProjectLabel>
                    FundingCountryOrProjectLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTRequestorName"
                    Title="CTRequestorNameLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="15%">
            <Template>
                @{
                    PLSRequestModel ctrequestorname = (PLSRequestModel)context;
                    <div id="target" title=" @ctrequestorname.CTRequestorName">
                        @ctrequestorname.CTRequestorName
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTRequestorNameLabel>
                    CTRequestorNameLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="StartDate"
                    Title="StartDate"
                    ShowFilterCellButtons="false" DisplayFormat="{0:dd/MMM/yyyy}"
                    Filterable="true" Width="16%">
            <Template>
                @{
                    PLSRequestModel startdate = (PLSRequestModel)context;
                    <div id="target" title="@startdate.StartDate?.ToString("dd-MMM-yyyy")">
                        @startdate.StartDate?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=StartDate>
                    StartDate
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CurrentStatusText"
                    Title="CurrentStatusLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="8%">
            <Template>
                @{
                    PLSRequestModel currentstatus = (PLSRequestModel)context;
                    <div id="target" title=" @currentstatus.CurrentStatusText">
                        @currentstatus.CurrentStatusText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CurrentStatusLabel>
                    CurrentStatusLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="RedFlagText"
                    Title="RegFlagLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="9%">
            <Template>
                @{
                    PLSRequestModel redflag = (PLSRequestModel)context;
                    <div id="target" title="@redflag.RedFlagText">
                        @redflag.RedFlagText
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=RegFlagLabel>
                    RegFlagLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridCommandColumn Title="" Width="12%">
            <GridCommandButton Command="Copy" Title="Copy"
                               ThemeColor=""
                               OnClick="@((args)=> OnPLSCopy(args.Item as PLSRequestModel))"
                               class="table-btn-icons-custom">
                <div class="copy-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
            <GridCommandButton Command="Delete" Title="Delete"
                               ThemeColor=""
                               class="table-btn-icons table-trash-icon">
                <div class="inactive-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-delete">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
    </GridColumns>
</TelerikGrid>
<TelerikGrid @ref="@GridRef_6"
             Data="@GridData"
             Pageable="true"
             PageSize="10"
             Sortable="true"
             ConfirmDelete="true"
             SortMode="@SortMode.Multiple"
             FilterMode="@GridFilterMode.FilterRow"
             Width="100%"
             Class="userprofilelisttable table-wrapper data-grid-table-wrapper">
    <GridColumns>
        <GridCommandColumn Title="" Width="5%">
            <GridCommandButton Command="Edit" Title="Edit"
                               ThemeColor=""
                               class="table-btn-icons"
                               OnClick="@((args)=>OnPLSEdit(args.Item as PLSRequestModel))">
                <div class="edit-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
        <GridColumn Field="AssociatedDTROrIRNumber"
                    Title="AssociatedDTROrIRNumberLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel dtrnumber = (PLSRequestModel)context;
                    <div id="target" title="@dtrnumber.AssociatedDTROrIRNumber">
                        @dtrnumber.AssociatedDTROrIRNumber
                    </div>
                }
            </Template>

            <HeaderTemplate>
                <div title=AssociatedDTROrIRNumberLabel>
                    AssociatedDTROrIRNumberLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="SRNumber"
                    Title="SRNumber"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel srnumber = (PLSRequestModel)context;
                    <div id="target" title="@srnumber.SRNumber">
                        @srnumber.SRNumber
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=SRNumber>
                    SRNumber
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="DateSubmitted"
                    Title="DateSubmitted"
                    ShowFilterCellButtons="false"
                    Filterable="true" DisplayFormat="{0:dd/MMM/yyyy}" Width="17%">
            <Template>
                @{
                    PLSRequestModel datesubmitted = (PLSRequestModel)context;
                    <div id="target" title="@datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")">
                        @datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=DateSubmitted>
                    Model.DateSubmitted
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="ProjectTitle"
                    Title="ProjectTitle"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel projecttitle = (PLSRequestModel)context;
                    <div id="target" title="@projecttitle.ProjectTitle">
                        @projecttitle.ProjectTitle
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=ProjectTitle>
                    ProjectTitle
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTFundingProgramText"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="16%"
                    Title="CTFundingProgramLabel">
            <Template>
                @{
                    PLSRequestModel ctfundingprgm = (PLSRequestModel)context;
                    <div id="target" title="@ctfundingprgm.CTFundingProgramText">
                        @ctfundingprgm.CTFundingProgramText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTFundingProgramLabel>
                    CTFundingProgramLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="FundingCountryOrProject"
                    Title="FundingCountryOrProjectLabel"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel fundingcp = (PLSRequestModel)context;
                    <div id="target" title="@fundingcp.FundingCountryOrProject">
                        @fundingcp.FundingCountryOrProject
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=FundingCountryOrProjectLabel>
                    FundingCountryOrProjectLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTRequestorName"
                    Title="CTRequestorNameLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="15%">
            <Template>
                @{
                    PLSRequestModel ctrequestorname = (PLSRequestModel)context;
                    <div id="target" title=" @ctrequestorname.CTRequestorName">
                        @ctrequestorname.CTRequestorName
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTRequestorNameLabel>
                    CTRequestorNameLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="StartDate"
                    Title="StartDate"
                    ShowFilterCellButtons="false" DisplayFormat="{0:dd/MMM/yyyy}"
                    Filterable="true" Width="16%">
            <Template>
                @{
                    PLSRequestModel startdate = (PLSRequestModel)context;
                    <div id="target" title="@startdate.StartDate?.ToString("dd-MMM-yyyy")">
                        @startdate.StartDate?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=StartDate>
                    StartDate
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CurrentStatusText"
                    Title="CurrentStatusLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="8%">
            <Template>
                @{
                    PLSRequestModel currentstatus = (PLSRequestModel)context;
                    <div id="target" title=" @currentstatus.CurrentStatusText">
                        @currentstatus.CurrentStatusText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CurrentStatusLabel>
                    CurrentStatusLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="RedFlagText"
                    Title="RegFlagLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="9%">
            <Template>
                @{
                    PLSRequestModel redflag = (PLSRequestModel)context;
                    <div id="target" title="@redflag.RedFlagText">
                        @redflag.RedFlagText
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=RegFlagLabel>
                    RegFlagLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridCommandColumn Title="" Width="12%">
            <GridCommandButton Command="Copy" Title="Copy"
                               ThemeColor=""
                               OnClick="@((args)=> OnPLSCopy(args.Item as PLSRequestModel))"
                               class="table-btn-icons-custom">
                <div class="copy-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
            <GridCommandButton Command="Delete" Title="Delete"
                               ThemeColor=""
                               class="table-btn-icons table-trash-icon">
                <div class="inactive-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-delete">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
    </GridColumns>
</TelerikGrid>
<TelerikGrid @ref="@GridRef_7"
             Data="@GridData"
             Pageable="true"
             PageSize="10"
             Sortable="true"
             ConfirmDelete="true"
             SortMode="@SortMode.Multiple"
             FilterMode="@GridFilterMode.FilterRow"
             Width="100%"
             Class="userprofilelisttable table-wrapper data-grid-table-wrapper">
    <GridColumns>
        <GridCommandColumn Title="" Width="5%">
            <GridCommandButton Command="Edit" Title="Edit"
                               ThemeColor=""
                               class="table-btn-icons"
                               OnClick="@((args)=>OnPLSEdit(args.Item as PLSRequestModel))">
                <div class="edit-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
        <GridColumn Field="AssociatedDTROrIRNumber"
                    Title="AssociatedDTROrIRNumberLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel dtrnumber = (PLSRequestModel)context;
                    <div id="target" title="@dtrnumber.AssociatedDTROrIRNumber">
                        @dtrnumber.AssociatedDTROrIRNumber
                    </div>
                }
            </Template>

            <HeaderTemplate>
                <div title=AssociatedDTROrIRNumberLabel>
                    AssociatedDTROrIRNumberLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="SRNumber"
                    Title="SRNumber"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel srnumber = (PLSRequestModel)context;
                    <div id="target" title="@srnumber.SRNumber">
                        @srnumber.SRNumber
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=SRNumber>
                    SRNumber
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="DateSubmitted"
                    Title="DateSubmitted"
                    ShowFilterCellButtons="false"
                    Filterable="true" DisplayFormat="{0:dd/MMM/yyyy}" Width="17%">
            <Template>
                @{
                    PLSRequestModel datesubmitted = (PLSRequestModel)context;
                    <div id="target" title="@datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")">
                        @datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=DateSubmitted>
                    Model.DateSubmitted
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="ProjectTitle"
                    Title="ProjectTitle"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel projecttitle = (PLSRequestModel)context;
                    <div id="target" title="@projecttitle.ProjectTitle">
                        @projecttitle.ProjectTitle
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=ProjectTitle>
                    ProjectTitle
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTFundingProgramText"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="16%"
                    Title="CTFundingProgramLabel">
            <Template>
                @{
                    PLSRequestModel ctfundingprgm = (PLSRequestModel)context;
                    <div id="target" title="@ctfundingprgm.CTFundingProgramText">
                        @ctfundingprgm.CTFundingProgramText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTFundingProgramLabel>
                    CTFundingProgramLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="FundingCountryOrProject"
                    Title="FundingCountryOrProjectLabel"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel fundingcp = (PLSRequestModel)context;
                    <div id="target" title="@fundingcp.FundingCountryOrProject">
                        @fundingcp.FundingCountryOrProject
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=FundingCountryOrProjectLabel>
                    FundingCountryOrProjectLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTRequestorName"
                    Title="CTRequestorNameLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="15%">
            <Template>
                @{
                    PLSRequestModel ctrequestorname = (PLSRequestModel)context;
                    <div id="target" title=" @ctrequestorname.CTRequestorName">
                        @ctrequestorname.CTRequestorName
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTRequestorNameLabel>
                    CTRequestorNameLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="StartDate"
                    Title="StartDate"
                    ShowFilterCellButtons="false" DisplayFormat="{0:dd/MMM/yyyy}"
                    Filterable="true" Width="16%">
            <Template>
                @{
                    PLSRequestModel startdate = (PLSRequestModel)context;
                    <div id="target" title="@startdate.StartDate?.ToString("dd-MMM-yyyy")">
                        @startdate.StartDate?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=StartDate>
                    StartDate
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CurrentStatusText"
                    Title="CurrentStatusLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="8%">
            <Template>
                @{
                    PLSRequestModel currentstatus = (PLSRequestModel)context;
                    <div id="target" title=" @currentstatus.CurrentStatusText">
                        @currentstatus.CurrentStatusText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CurrentStatusLabel>
                    CurrentStatusLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="RedFlagText"
                    Title="RegFlagLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="9%">
            <Template>
                @{
                    PLSRequestModel redflag = (PLSRequestModel)context;
                    <div id="target" title="@redflag.RedFlagText">
                        @redflag.RedFlagText
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=RegFlagLabel>
                    RegFlagLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridCommandColumn Title="" Width="12%">
            <GridCommandButton Command="Copy" Title="Copy"
                               ThemeColor=""
                               OnClick="@((args)=> OnPLSCopy(args.Item as PLSRequestModel))"
                               class="table-btn-icons-custom">
                <div class="copy-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
            <GridCommandButton Command="Delete" Title="Delete"
                               ThemeColor=""
                               class="table-btn-icons table-trash-icon">
                <div class="inactive-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-delete">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
    </GridColumns>
</TelerikGrid>
<TelerikGrid @ref="@GridRef_8"
             Data="@GridData"
             Pageable="true"
             PageSize="10"
             Sortable="true"
             ConfirmDelete="true"
             SortMode="@SortMode.Multiple"
             FilterMode="@GridFilterMode.FilterRow"
             Width="100%"
             Class="userprofilelisttable table-wrapper data-grid-table-wrapper">
    <GridColumns>
        <GridCommandColumn Title="" Width="5%">
            <GridCommandButton Command="Edit" Title="Edit"
                               ThemeColor=""
                               class="table-btn-icons"
                               OnClick="@((args)=>OnPLSEdit(args.Item as PLSRequestModel))">
                <div class="edit-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
        <GridColumn Field="AssociatedDTROrIRNumber"
                    Title="AssociatedDTROrIRNumberLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel dtrnumber = (PLSRequestModel)context;
                    <div id="target" title="@dtrnumber.AssociatedDTROrIRNumber">
                        @dtrnumber.AssociatedDTROrIRNumber
                    </div>
                }
            </Template>

            <HeaderTemplate>
                <div title=AssociatedDTROrIRNumberLabel>
                    AssociatedDTROrIRNumberLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="SRNumber"
                    Title="SRNumber"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel srnumber = (PLSRequestModel)context;
                    <div id="target" title="@srnumber.SRNumber">
                        @srnumber.SRNumber
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=SRNumber>
                    SRNumber
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="DateSubmitted"
                    Title="DateSubmitted"
                    ShowFilterCellButtons="false"
                    Filterable="true" DisplayFormat="{0:dd/MMM/yyyy}" Width="17%">
            <Template>
                @{
                    PLSRequestModel datesubmitted = (PLSRequestModel)context;
                    <div id="target" title="@datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")">
                        @datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=DateSubmitted>
                    Model.DateSubmitted
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="ProjectTitle"
                    Title="ProjectTitle"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel projecttitle = (PLSRequestModel)context;
                    <div id="target" title="@projecttitle.ProjectTitle">
                        @projecttitle.ProjectTitle
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=ProjectTitle>
                    ProjectTitle
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTFundingProgramText"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="16%"
                    Title="CTFundingProgramLabel">
            <Template>
                @{
                    PLSRequestModel ctfundingprgm = (PLSRequestModel)context;
                    <div id="target" title="@ctfundingprgm.CTFundingProgramText">
                        @ctfundingprgm.CTFundingProgramText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTFundingProgramLabel>
                    CTFundingProgramLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="FundingCountryOrProject"
                    Title="FundingCountryOrProjectLabel"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel fundingcp = (PLSRequestModel)context;
                    <div id="target" title="@fundingcp.FundingCountryOrProject">
                        @fundingcp.FundingCountryOrProject
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=FundingCountryOrProjectLabel>
                    FundingCountryOrProjectLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTRequestorName"
                    Title="CTRequestorNameLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="15%">
            <Template>
                @{
                    PLSRequestModel ctrequestorname = (PLSRequestModel)context;
                    <div id="target" title=" @ctrequestorname.CTRequestorName">
                        @ctrequestorname.CTRequestorName
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTRequestorNameLabel>
                    CTRequestorNameLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="StartDate"
                    Title="StartDate"
                    ShowFilterCellButtons="false" DisplayFormat="{0:dd/MMM/yyyy}"
                    Filterable="true" Width="16%">
            <Template>
                @{
                    PLSRequestModel startdate = (PLSRequestModel)context;
                    <div id="target" title="@startdate.StartDate?.ToString("dd-MMM-yyyy")">
                        @startdate.StartDate?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=StartDate>
                    StartDate
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CurrentStatusText"
                    Title="CurrentStatusLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="8%">
            <Template>
                @{
                    PLSRequestModel currentstatus = (PLSRequestModel)context;
                    <div id="target" title=" @currentstatus.CurrentStatusText">
                        @currentstatus.CurrentStatusText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CurrentStatusLabel>
                    CurrentStatusLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="RedFlagText"
                    Title="RegFlagLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="9%">
            <Template>
                @{
                    PLSRequestModel redflag = (PLSRequestModel)context;
                    <div id="target" title="@redflag.RedFlagText">
                        @redflag.RedFlagText
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=RegFlagLabel>
                    RegFlagLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridCommandColumn Title="" Width="12%">
            <GridCommandButton Command="Copy" Title="Copy"
                               ThemeColor=""
                               OnClick="@((args)=> OnPLSCopy(args.Item as PLSRequestModel))"
                               class="table-btn-icons-custom">
                <div class="copy-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
            <GridCommandButton Command="Delete" Title="Delete"
                               ThemeColor=""
                               class="table-btn-icons table-trash-icon">
                <div class="inactive-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-delete">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
    </GridColumns>
</TelerikGrid>
<TelerikGrid @ref="@GridRef_9"
             Data="@GridData"
             Pageable="true"
             PageSize="10"
             Sortable="true"
             ConfirmDelete="true"
             SortMode="@SortMode.Multiple"
             FilterMode="@GridFilterMode.FilterRow"
             Width="100%"
             Class="userprofilelisttable table-wrapper data-grid-table-wrapper">
    <GridColumns>
        <GridCommandColumn Title="" Width="5%">
            <GridCommandButton Command="Edit" Title="Edit"
                               ThemeColor=""
                               class="table-btn-icons"
                               OnClick="@((args)=>OnPLSEdit(args.Item as PLSRequestModel))">
                <div class="edit-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
        <GridColumn Field="AssociatedDTROrIRNumber"
                    Title="AssociatedDTROrIRNumberLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel dtrnumber = (PLSRequestModel)context;
                    <div id="target" title="@dtrnumber.AssociatedDTROrIRNumber">
                        @dtrnumber.AssociatedDTROrIRNumber
                    </div>
                }
            </Template>

            <HeaderTemplate>
                <div title=AssociatedDTROrIRNumberLabel>
                    AssociatedDTROrIRNumberLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="SRNumber"
                    Title="SRNumber"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel srnumber = (PLSRequestModel)context;
                    <div id="target" title="@srnumber.SRNumber">
                        @srnumber.SRNumber
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=SRNumber>
                    SRNumber
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="DateSubmitted"
                    Title="DateSubmitted"
                    ShowFilterCellButtons="false"
                    Filterable="true" DisplayFormat="{0:dd/MMM/yyyy}" Width="17%">
            <Template>
                @{
                    PLSRequestModel datesubmitted = (PLSRequestModel)context;
                    <div id="target" title="@datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")">
                        @datesubmitted.DateSubmitted?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=DateSubmitted>
                    Model.DateSubmitted
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="ProjectTitle"
                    Title="ProjectTitle"
                    ShowFilterCellButtons="false" Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel projecttitle = (PLSRequestModel)context;
                    <div id="target" title="@projecttitle.ProjectTitle">
                        @projecttitle.ProjectTitle
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=ProjectTitle>
                    ProjectTitle
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTFundingProgramText"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="16%"
                    Title="CTFundingProgramLabel">
            <Template>
                @{
                    PLSRequestModel ctfundingprgm = (PLSRequestModel)context;
                    <div id="target" title="@ctfundingprgm.CTFundingProgramText">
                        @ctfundingprgm.CTFundingProgramText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTFundingProgramLabel>
                    CTFundingProgramLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="FundingCountryOrProject"
                    Title="FundingCountryOrProjectLabel"
                    ShowFilterCellButtons="false"
                    Filterable="true" Width="12%">
            <Template>
                @{
                    PLSRequestModel fundingcp = (PLSRequestModel)context;
                    <div id="target" title="@fundingcp.FundingCountryOrProject">
                        @fundingcp.FundingCountryOrProject
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=FundingCountryOrProjectLabel>
                    FundingCountryOrProjectLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CTRequestorName"
                    Title="CTRequestorNameLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="15%">
            <Template>
                @{
                    PLSRequestModel ctrequestorname = (PLSRequestModel)context;
                    <div id="target" title=" @ctrequestorname.CTRequestorName">
                        @ctrequestorname.CTRequestorName
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CTRequestorNameLabel>
                    CTRequestorNameLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="StartDate"
                    Title="StartDate"
                    ShowFilterCellButtons="false" DisplayFormat="{0:dd/MMM/yyyy}"
                    Filterable="true" Width="16%">
            <Template>
                @{
                    PLSRequestModel startdate = (PLSRequestModel)context;
                    <div id="target" title="@startdate.StartDate?.ToString("dd-MMM-yyyy")">
                        @startdate.StartDate?.ToString("dd-MMM-yyyy")
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=StartDate>
                    StartDate
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="CurrentStatusText"
                    Title="CurrentStatusLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="8%">
            <Template>
                @{
                    PLSRequestModel currentstatus = (PLSRequestModel)context;
                    <div id="target" title=" @currentstatus.CurrentStatusText">
                        @currentstatus.CurrentStatusText
                    </div>

                }
            </Template>
            <HeaderTemplate>
                <div title=CurrentStatusLabel>
                    CurrentStatusLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridColumn Field="RedFlagText"
                    Title="RegFlagLabel"
                    ShowFilterCellButtons="false" Filterable="true" Width="9%">
            <Template>
                @{
                    PLSRequestModel redflag = (PLSRequestModel)context;
                    <div id="target" title="@redflag.RedFlagText">
                        @redflag.RedFlagText
                    </div>
                }
            </Template>
            <HeaderTemplate>
                <div title=RegFlagLabel>
                    RegFlagLabel
                </div>
            </HeaderTemplate>
        </GridColumn>
        <GridCommandColumn Title="" Width="12%">
            <GridCommandButton Command="Copy" Title="Copy"
                               ThemeColor=""
                               OnClick="@((args)=> OnPLSCopy(args.Item as PLSRequestModel))"
                               class="table-btn-icons-custom">
                <div class="copy-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-edit">
                </TelerikTooltip>
            </GridCommandButton>
            <GridCommandButton Command="Delete" Title="Delete"
                               ThemeColor=""
                               class="table-btn-icons table-trash-icon">
                <div class="inactive-icon"></div>
                <TelerikTooltip TargetSelector=".btn>GridCommandButton[Title]"
                                Position="@TooltipPosition.Right"
                                ShowOn="@TooltipShowEvent.Hover"
                                Id="position-tooltip-delete">
                </TelerikTooltip>
            </GridCommandButton>
        </GridCommandColumn>
    </GridColumns>
</TelerikGrid>

<TelerikTooltip TargetSelector="#target"
                Position="@TooltipPosition.Top">
</TelerikTooltip>


@code {
    private Component component = new Component();
    // Track whether Dispose has been called.
    private bool disposed = false;
    private PLSRequestModel Model { get; set; } = default!;
    private TelerikGrid<PLSRequestModel>? GridRef { get; set; } = default!;
    private TelerikGrid<PLSRequestModel>? GridRef_1 { get; set; } = default!;
    private TelerikGrid<PLSRequestModel>? GridRef_2 { get; set; } = default!;
    private TelerikGrid<PLSRequestModel>? GridRef_3 { get; set; } = default!;
    private TelerikGrid<PLSRequestModel>? GridRef_4 { get; set; } = default!;
    private TelerikGrid<PLSRequestModel>? GridRef_5 { get; set; } = default!;
    private TelerikGrid<PLSRequestModel>? GridRef_6 { get; set; } = default!;
    private TelerikGrid<PLSRequestModel>? GridRef_7 { get; set; } = default!;
    private TelerikGrid<PLSRequestModel>? GridRef_8 { get; set; } = default!;
    private TelerikGrid<PLSRequestModel>? GridRef_9 { get; set; } = default!;
    List<PLSRequestModel> GridData { get; set; } = new List<PLSRequestModel>();
    public void OnPLSEdit(PLSRequestModel item)
    {

    }
    public void OnPLSCopy(PLSRequestModel item)
    {
    }
    private async void OnPLSDelete(Guid Id)
    {
    }
    protected override async Task OnParametersSetAsync()
    {

    }
    private async Task Refreshgrid()
    {
        Console.WriteLine("Refreshgrid...");

        GridRef.Rebind();
        await Task.CompletedTask;
    }
    protected override void OnInitialized()
    {
        Console.WriteLine("OnInitialized...");

        GridData = Enumerable.Range(1, 1000).Select(x => new PLSRequestModel
            {

                PLSNumber = "pls number" + x,
                SRNumber = "SR Number" + x,
                WorkOrderNumber = "WorkOrderNumber" + x,
                AssociatedDTROrIRNumber = "AssociatedDTROrIRNumber" + x,
                TeamOrEventName = "Team/EventName" + x,
                CurrentStatusText = "CurrentStatus" + x,
                RedFlagText = "Yes",
                DestinationCity = "City" + x,
                StartDate = DateTime.Now.AddMonths(-x).Date,
                CTRequestorName = "CTR" + x,
                FundingCountryOrProject = "Country/Project" + x,
                CTFundingProgramText = "CTRProgram" + x

            }).ToList();
    }

    protected async Task OnReadItems(GridReadEventArgs args)
    {
        try
        {
            Console.WriteLine("OnReadItems...");
            GridData = Enumerable.Range(1, 10000).Select(x => new PLSRequestModel
                {

                    PLSNumber = "pls number" + x,
                    SRNumber = "SR Number" + x,
                    WorkOrderNumber = "WorkOrderNumber" + x,
                    AssociatedDTROrIRNumber = "AssociatedDTROrIRNumber" + x,
                    TeamOrEventName = "Team/EventName" + x,
                    CurrentStatusText = "CurrentStatus" + x,
                    RedFlagText = "Yes",
                    DestinationCity = "City" + x,
                    StartDate = DateTime.Now.AddMonths(-x).Date,
                    CTRequestorName = "CTR" + x,
                    FundingCountryOrProject = "Country/Project" + x,
                    CTFundingProgramText = "CTRProgram" + x

                }).ToList();

            args.Data = GridData;
            StateHasChanged();
            await Task.CompletedTask;
        }
        catch (Exception ex)
        {

        }
    }
    public class PLSRequestModel
    {
        public Guid? Id { get; set; }
        public Guid? CreatedBy { get; set; }
        public Guid? UserId { get; set; }

        public string? PLSNumber { get; set; }
        public string? SRNumber { get; set; }
        public string? WorkOrderNumber { get; set; }
        public string? AssociatedDTROrIRNumber { get; set; }
        public string? TeamOrEventName { get; set; }
        public Guid? CurrentStatus { get; set; }
        public string? CurrentStatusText { get; set; }
        public bool RedFlag { get; set; }
        public string? RedFlagText { get; set; }
        public Guid? DestinationCountry { get; set; }
        public string? DestinationCity { get; set; }
        public DateTime? StartDate { get; set; }
        public DateTime? EndDate { get; set; }
        public Guid? CTFundingProgram { get; set; }
        public string? CTFundingProgramText { get; set; }
        public int? TimeZone { get; set; }
        public decimal? ToatlCost { get; set; }
        public string? FundingCountryOrProject { get; set; }
        public string? CTRequestorName { get; set; }
        public string? Revision { get; set; }
        public string? CTProjectManagerName { get; set; }
        public string? CTRequestorEmail { get; set; }
        public string? ApprovedBy { get; set; }
        public string? CTProjectManagerEmail { get; set; }
        public string? CTRequestorPhone { get; set; }
        public DateTime? DateSubmitted { get; set; }
        public string? CTProjectManagerPhone { get; set; }
        public string? TTIPOCEmail { get; set; }
        public string? TTIPOCPhone { get; set; }
        public string? TTIPOCName { get; set; }
        public string? TTIAlternatePOCEmail { get; set; }
        public string? TTIAlternatePOCPhone { get; set; }
        public string? TTIAlternatePOCName { get; set; }
        public string? TypeOfTravel { get; set; }
        public string? PurposeOrRequestSummary { get; set; }
        public int? IsDelete { get; set; }
        public bool? VettingOfForegnNationParticipants { get; set; }
        public DateTime? Date { get; set; }
        public bool? AreWriitenDocPresentToForegnNatinls { get; set; }
        public string? SpecbackgroundexpOrTranslateQualification { get; set; }
        public string? DocumentDestinationCountry { get; set; }
        public bool? TranslatorCONUSBased { get; set; }
        public bool? TranslatorUSCitizen { get; set; }
        public bool? IsMaterialApprovedForExport { get; set; }
        public bool? ContainPIIOfUSCitizen { get; set; }
        public bool? ContainPIIOfForegnNationals { get; set; }
        public bool? ContainFOUOandCUI { get; set; }
        public string? AdditionalRequirementsandLimitation { get; set; }
        public string? ProjectTitle { get; set; }

        #region For labels
        public string? AssociatedDTROrIRNumberLabel { get; set; }
        public string? CTFundingProgramLabel { get; set; }
        public string? FundingCountryOrProjectLabel { get; set; }
        public string? CTRequestorNameLabel { get; set; }
        public string? PLSListCardTitile { get; set; }
        public string? PLSRequestFormCardTitile { get; set; }
        public string? PLSRestrictionsCardTitile { get; set; }
        public string? RegFlagLabel { get; set; }
        public string? CurrentStatusLabel { get; set; }
        #endregion

        #region Grid Action
        public string? Action { get; set; }
        public string? Edit { get; set; }
        public string? Delete { get; set; }
        public string? Copy { get; set; }
        #endregion

    }
    #region Object Dispose
    // Other managed resource this class uses.


    void IDisposable.Dispose()
    {
        Dispose(disposing: true);
        // This object will be cleaned up by the Dispose method.
        // Therefore, you should call GC.SuppressFinalize to
        // take this object off the finalization queue
        // and prevent finalization code for this object
        // from executing a second time.
        GC.SuppressFinalize(this);
        if (GC_Collect)
        {
            GCSettings.LargeObjectHeapCompactionMode = GCLargeObjectHeapCompactionMode.CompactOnce;
            //GC.Collect();
            GC.Collect();
            GC.WaitForPendingFinalizers();
            GC.Collect();
        }
    }
    private int currentCount = 0;
    private bool GC_Collect = false;

    //protected override bool ShouldRender()
    //{
    //    return shouldRender;
    //}
    protected virtual void Dispose(bool disposing)
    {
        if (!this.disposed)
        {
            // If disposing equals true, dispose all managed
            // and unmanaged resources.
            if (disposing)
            {
                // Dispose managed resources.
                component.Dispose();
                if (GridRef is not null)
                    GridRef?.Dispose();
                if (GridRef_1 is not null)
                    GridRef_1?.Dispose();
                if (GridRef_2 is not null)
                    GridRef_2?.Dispose();
                if (GridRef_3 is not null)
                    GridRef_3?.Dispose();
                if (GridRef_4 is not null)
                    GridRef_4?.Dispose();
                if (GridRef_5 is not null)
                    GridRef_5?.Dispose();
                if (GridRef_6 is not null)
                    GridRef_6?.Dispose();
                if (GridRef_7 is not null)
                    GridRef_7?.Dispose();
                if (GridRef_7 is not null)
                    GridRef_7?.Dispose();
                if (GridRef_8 is not null)
                    GridRef_8?.Dispose();
                if (GridRef_9 is not null)
                    GridRef_9?.Dispose();

                GridData = null;

            }

            // Call the appropriate methods to clean up
            // unmanaged resources here.
            // If disposing is false,
            // only the following code is executed.


            // Note disposing has been done.
            disposed = true;
        }
    }
    //public void Dispose()
    //{
    //    if (objReference is not null)
    //        objReference?.Dispose();

    //    if (unitofWork is not null)
    //        unitofWork.Dispose();

    //    ProgramListForm = null;
    //    StatusDropDownList = null;
    //}
    #endregion
}
